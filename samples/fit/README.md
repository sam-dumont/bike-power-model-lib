# Sample FIT files

Small, curated Garmin Power Guide FIT files (messages 352/353) that document
the format the writer targets ship here alongside the engine.

They are format references only: no personal ride data (GPS, heart rate,
power). The `.gitignore` blocks `*.fit` everywhere except this directory, so an
activity FIT can't be committed by accident.
